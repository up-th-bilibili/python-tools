# SimpleMarkdown 语法总结

这是一个简单的 Markdown 变体，由 smdc.py 实现。它支持以下语法。

## 块级语法

### 标题

行首一个或多个 `#` 表示标题。`#` 的数量决定级别，无上限。  
`#` 后可以有空格，也可以直接跟内容。

### 无序列表

行首 `-` 表示无序列表项。

- 第一项
- 第二项
- 第三项

### 有序列表

行首数字加 `.` 表示有序列表项。数字可以不连续。

1. 第一项
2. 第二项
3. 第三项

### 换行

行尾两个空格加换行会生成换行符。  
这一行和上一行之间会有一个换行。

## 行内语法

### 加粗

使用 `**` 或 `^^` 包围文本。

**这是加粗**，^^这也是加粗^^。

### 斜体

使用 `*` 或 `^` 包围文本。

*这是斜体*，^这也是斜体^。

### 行内代码

使用反引号包围代码。

`这是行内代码`。

### 转义

反斜杠可以转义特殊字符：\*、\\、\`、\^、\#。

### HTML 特殊字符转义

<、>、& 会被自动转义。

## 暂不支持的语法

- 代码块
- 链接
- 图片
- 引用块
- 水平线
- 表格
- 嵌套列表

## 扩展与样式

- 使用 `-load-extension=xxx.json` 加载扩展。
- 使用 `-style=xxx` 设置样式。
- 内置样式：`default`、`pure`、`document`、`tech`。

## 特殊行为

- 列表项是单行的，不能跨行。
- 列表项之间可以有空行。
- 强调标记支持简单嵌套，例如 **粗 *斜* 粗**。
- 标题内部不解析行内标记。
- 列表项内部不解析行内标记。
